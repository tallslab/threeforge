/** Copies three's Draco glTF decoder and Basis transcoder to `<dir>/draco` and `<dir>/basis` (the mjs twin of `src/cli/decoders.ts`). */
export function copyDecoders(dir: string, libs?: string): void;
